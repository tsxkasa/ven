#pragma once

#include "pch.h"

namespace gui {
namespace render {
bool exit_button();
void framerate(double fps);
void cursor_pos(double x, double y);
void object_amt(int object_num);
}; // namespace render
}; // namespace gui